import { Paginize } from "./dev"
// import { paginationLib } from "./libCheck"

function App() {
  // running dev
  const pagination = new Paginize(".paginize", {
    perPage: 8,
  })

  // running lib
  // paginationLib()
}

export default App()
